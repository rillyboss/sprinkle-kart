{
  "name": "sprinkle-kart",
  "version": "2.0.1",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview --open",
    "test": "vitest run",
    "smoke": "node scripts/smoke.mjs",
    "test:coverage": "vitest run --coverage",
    "worker:dev": "node scripts/worker.mjs dev",
    "worker:test": "node scripts/worker.mjs test",
    "worker:deploy": "node scripts/worker.mjs deploy",
    "worker:login": "node scripts/worker.mjs login",
    "worker:secret": "node scripts/worker.mjs secret"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Sprinkle Kart — a cute, family-friendly 3D split-screen kart racer made for Sophia",
  "dependencies": {
    "three": "^0.186.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "playwright": "^1.63.0",
    "vite": "^8.3.1",
    "vitest": "^4.1.11"
  },
  "type": "module",
  "private": true
}
